/*
 *
 * Implement a quick sort function.
 *
 * Learn more here: https://en.wikipedia.org/wiki/Quicksort
 * 
 */

var quickSort = function quickSort(array, left, right) {
  // initialize pivot as last element in array
  // loop through array
    // if item is less than or equal to pivot
      // push item to left
    // else
      // push item to right
  // return a concatenation of a quickSort of left and quickSort of right
};
